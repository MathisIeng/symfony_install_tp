{% extends 'base.html.twig' %}

{% block metatitle %}

    {{ article.title }} | SIUUUUUU

{% endblock metatitle %}

{% block main %}

    <ul>
        {% for i in 1..5 %}
            <li>
                {% if article %}
                    <h1>{{ article.title }}</h1>
                    <p>{{ article.content }}</p>
                    <img src="{{ article.image }}" alt="Image de {{ article.title }}">
                {% else %}
                    <p>On a rien trouvé chef</p>
                {% endif %}
            </li>
        {% endfor %}
    </ul>

{% endblock main %}
